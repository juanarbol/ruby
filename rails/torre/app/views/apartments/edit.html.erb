<h1>Editar apartamento</h1>

<%= form_for @apartment do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :floor %>
	<%= f.text_field :floor %>
	<%= f.label :tower_id %>
	<%= f.select :tower_id, Tower.all.collect{|u| [u.name,u.id]} %>
	<%= f.submit %>
<% end %>	