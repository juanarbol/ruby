<h1>Crear nuevo apartamento</h1>
<%= link_to "Ir a root", root_path %>
<%= form_for @apartment do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :floor %>
	<%= f.text_field :floor %>
	<%= f.submit %>
<% end %>