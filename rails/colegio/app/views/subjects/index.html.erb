<h1>Todas las asignatura</h1>
<%= link_to "Nueva materia", new_subject_path%>
<ul>
<% @subject.each do |f| %>
	<li>
		<%= f.name %>
		<%= link_to "Eliminar", subject_path(f) , method: :delete, data: { confirm: "Sure?"} %>
		<%= link_to "Editar", edit_subject_path(f)%>
	</li>
<% end %>
</ul>